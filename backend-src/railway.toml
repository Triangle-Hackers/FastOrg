[build]
builder = "nixpacks"
buildCommand = """
python -m pip install --upgrade pip
pip install wheel
pip install -r requirements.txt
"""

[deploy]
startCommand = "uvicorn mainapi:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"

[variables]
PYTHON_VERSION = "3.9.16"