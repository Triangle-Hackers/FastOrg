{
  "name": "fastorg-backend",
  "version": "1.0.0",
  "description": "Python backend service",
  "scripts": {
    "start": "uvicorn mainapi:app --host 0.0.0.0 --port ${PORT:-8000}",
    "build": "pip install -r requirements.txt",
    "dev": "uvicorn mainapi:app --reload --host localhost --port 8000"
  },
  "engines": {
    "node": ">=14.0.0",
    "python": "3.x"
  },
  "dependencies": {},
  "devDependencies": {}
}